{
  // Tailwind v4 at-rules (@theme, @apply, @utility, ...) aren't known to the
  // built-in CSS language server, which flags them as "Unknown at rule".
  "css.lint.unknownAtRules": "ignore",
  "files.associations": {
    "*.css": "tailwindcss"
  }
}
